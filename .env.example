# DGX Web UI Deployment Configuration
# Copy this file to .env and fill in your values

# Target server configuration
DGX_HOST=your-server-ip-or-hostname
DGX_USER=your-username
DGX_PASS=your-password  # Optional if using SSH keys

# Remote deployment directory (optional)
# REMOTE_DIR=/home/$USER/dgx-web-ui

# Network configuration for frontend display (optional)
# These are displayed in the Dashboard/Status pages
# LOCAL_NETWORK_IP=192.168.1.xxx
# VPN_NETWORK_IP=100.xxx.xxx.xxx
